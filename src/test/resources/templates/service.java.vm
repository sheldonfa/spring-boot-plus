package ${package.Service};

import com.baomidou.mybatisplus.core.metadata.IPage;
import ${package.Entity}.${entity};
import ${superServiceClassPackage};
import ${cfg.queryParamPath};
import ${cfg.queryVoPath};
import ${cfg.paging};

import java.io.Serializable;
import java.util.List;

/**
 * <pre>
 * $!{table.comment} 服务类
 * </pre>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entity}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entity}> {
#if(${cfg.generatorStrategy} != 'SIMPLE')

    /**
     * 保存
     *
     * @param param
     * @return true or false
     */
    boolean save${entity}(${entity}AddParam param);

    /**
     * 修改
     *
     * @param param
     * @return true or false
     */
    boolean update${entity}(${entity}UpdateParam param);

    /**
     * 删除
     *
     * @param id
     * @return
     * @throws Exception
     */
    boolean delete${entity}(Long id);


    /**
     * 根据ID获取查询对象
     *
     * @param id
     * @return
     * @throws Exception
     */
    ${entity} get${entity}ByPk(Serializable id);

    /**
    * 获取列表对象
    *
    * @param queryParam
    * @return
    */
    List<${entity}> list(${entity}QueryParam queryParam);

    /**
     * 获取分页对象
     *
     * @param queryParam 请求参数
     * @return
     */
     IPage<${entity}> pageList(${entity}QueryParam queryParam);
#end

}
#end
