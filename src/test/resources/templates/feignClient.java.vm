package ${cfg.feignClientPath};

import ${package.Entity}.${entity};
import ${package.Service}.${table.serviceName};
import ${cfg.queryParamPath};
import ${cfg.queryVoPath};
import ${cfg.dtoPath};
import ${cfg.dtoPath};
import ${cfg.convertPath};
import ${cfg.apiResult};
#if(${superControllerClassPackage})
import ${superControllerClassPackage};
#end
#if(${swagger2})
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
#end
import lombok.extern.slf4j.Slf4j;
import io.geekidea.springbootplus.common.constraints.MessageCode;
import org.apache.shiro.authz.annotation.RequiresPermissions;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.core.metadata.IPage;
import io.geekidea.springbootplus.util.CommonUtils;
import io.geekidea.springbootplus.util.ExceptionUtil;
#if(${cfg.paramValidation})
import javax.validation.Valid;
#end
import ${cfg.paging};
import ${cfg.idParamPath};

/**
 * <pre>
 * $!{table.comment} 前端控制器
 * </pre>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
class ${table.controllerName}#if(${superControllerClass}) : ${superControllerClass}()#end

#else
public interface  ${table.controllerName}Client {

#if(${cfg.generatorStrategy} != 'SIMPLE')
    /**
     * 添加$!{table.comment}
     */
    @PostMapping("/${cfg.colonTableName}/add")
#if(${cfg.requiresPermissions})
    @RequiresPermissions("$!{cfg.colonTableName}:add")
#end
    ResponseWrapper<${entity}AddVo> add${entity}(#if(${cfg.paramValidation})@Valid#end @RequestBody ${entity}AddParam ${cfg.entityObjectName}Dto);

    /**
     * 修改$!{table.comment}
     */
    @PostMapping("/${cfg.colonTableName}/update")
#if(${cfg.requiresPermissions})
    @RequiresPermissions("$!{cfg.colonTableName}:update")
#end
    ResponseWrapper<Boolean> update${entity}(#if(${cfg.paramValidation})@Valid#end @RequestBody ${entity}Dto ${cfg.entityObjectName}Dto);

    /**
     * 删除$!{table.comment}
     */
    @DeleteMapping("/${cfg.colonTableName}/delete/{recordId}")
#if(${cfg.requiresPermissions})
    @RequiresPermissions("$!{cfg.colonTableName}:delete")
#end
    ResponseWrapper<Boolean> delete${entity}(@PathVariable("recordId") String recordId);

    /**
     * 获取$!{table.comment}
     */
    @GetMapping("/${cfg.colonTableName}/info/{recordId}")
#if(${cfg.requiresPermissions})
    @RequiresPermissions("$!{cfg.colonTableName}:info")
#end
    ResponseWrapper<${entity}Dto> get${entity}ByPk(@PathVariable("recordId") String recordId);

    /**
     * $!{table.comment}列表
     */
    @PostMapping("/${cfg.colonTableName}/list")
    ResponseWrapper<${entity}QueryListResultDto> get${entity}List(#if(${cfg.paramValidation})@Valid#end @RequestBody ${entity}QueryParam ${cfg.entityObjectName}QueryParam);

    /**
     * $!{table.comment}分页列表
     */
    @PostMapping("/${cfg.colonTableName}/pageList")
    ResponseWrapper<Page<${entity}QueryResultDto>> get${entity}PageList(#if(${cfg.paramValidation})@Valid#end @RequestBody ${entity}QueryParam ${cfg.entityObjectName}QueryParam);
#end

}

#end